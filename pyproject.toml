[project]
name = "simulator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.8",
    "setuptools>=82.0.1",
]

[project.optional-dependencies]
docs = [
    "sphinx>=9.0",
    "numpydoc",
    "furo",
]
